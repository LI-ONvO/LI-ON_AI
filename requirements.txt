# --- 런타임 ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# LLM
# langchain 우산 패키지는 쓰지 않는다. 필요한 건 core와 openai 바인딩뿐이다.
langchain-core>=0.3.15,<0.4.0
langchain-openai==0.2.6
openai>=1.54.0,<2.0.0

# 자격증 DB (배치가 채워둔 참조 데이터를 읽기만 한다)
PyMySQL==1.1.1

# 배치에서 공공데이터 API 호출용
requests==2.32.3

# --- 개발/테스트 ---
pytest==8.3.3
httpx==0.27.2
